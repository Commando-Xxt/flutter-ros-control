name: week5_flutter_ros
description: Minimal Flutter + ROS control via rosbridge
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.5
  http: ^1.2.2
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
